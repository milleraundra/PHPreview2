<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Count Repeat</title>
    </head>
    <body>
        <h1>Count Repeat</h1>
        <h2>A Simple Search for Successive Sentence Sections.</h2>
        <form id="form" class="" action="/runCount" method="get">
            <label for="sentence">Enter a word, phrase, or sentence:</label>
            <textarea name="sentence" rows="8" cols="20"></textarea>
            <label for="word">Enter a single word search:</label>
            <input type="text" name="word" value="">
            <button type="submit" name="button">Search</button>
        </form>
        <p>
            {% if result is not empty %}
                {{ result }}
            {% endif %}
        </p>

    </body>
</html>
